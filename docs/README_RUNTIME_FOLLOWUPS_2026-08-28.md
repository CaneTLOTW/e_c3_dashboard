# Runtime follow-ups – 2026-08-28

This index separates the three findings from the post-0.5.32 visual review:

1. **LIVE hero image still blank** – existing Issue #5; 0.5.33 follow-up in `BUG_5_LIVE_HERO_LAYOUT_REGRESSION_0.5.32.md`.
2. **GPS date selector vs full orange archive overlay** – `BUG_16_GPS_ARCHIVE_OVERLAY_DATE_FILTER.md`; separate functional regression.
3. **Remote connection / remote command values unavailable in both new and legacy dashboards** – `DIAG_REMOTE_CHANNEL_UNAVAILABLE_2026-08-28.md`; classify upstream/capability first, not as a dashboard regression without evidence.

The user-provided GPS screenshot is intentionally not committed because it contains private coordinates and travel tracks.
